# Spring-Boot

### `demo`

Classic "Hello World!" endpoint. You can even tell it your name, and it will respond in a more friendly way if you add `?name={your_name}`to the end of the URL.

For this project, the `Spring Web` dependency was used to build web applications, including RESTful, using Spring MVC.